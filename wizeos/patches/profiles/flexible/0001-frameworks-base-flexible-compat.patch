diff --git a/frameworks/base/core/java/android/ext/settings/ExtSettings.java b/frameworks/base/core/java/android/ext/settings/ExtSettings.java
index 821f3d2..3d28cd5 100644
--- a/frameworks/base/core/java/android/ext/settings/ExtSettings.java
+++ b/frameworks/base/core/java/android/ext/settings/ExtSettings.java
@@ -24,3 +24,6 @@ public class ExtSettings {
 public class ExtSettings {
 
+    // WizeOS Flexible: keep the classic fork path by default for LSPosed / module compatibility.
+    // In ZygoteConnection, a true value selects forkAndSpecialize instead of the forkSimpleApps
+    // path that later enters ExecInit with IS_EXEC_SPAWNED_APP_PROCESS=1.
     public static final BoolSysProperty EXEC_SPAWNING = new BoolSysProperty(
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswDenyNativeDebug.java b/frameworks/base/core/java/android/ext/settings/app/AswDenyNativeDebug.java
index 0e2b1fc..1f7a1d2 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswDenyNativeDebug.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswDenyNativeDebug.java
@@ -8,5 +8,4 @@ import android.content.pm.GosPackageStateFlag;
 import android.content.pm.GosPackageStateFlag;
 import android.ext.settings.ExtSettings;
 
-import com.android.internal.os.SELinuxFlags;
 import com.android.server.os.nano.AppCompatProtos;
@@ -27,12 +26,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp() && !SELinuxFlags.isSystemAppSepolicyWeakeningAllowed()) {
-            si.immutabilityReason = IR_IS_SYSTEM_APP;
-            return true;
-        }
-
-        if (ps.hasFlag(GosPackageStateFlag.ENABLE_EXPLOIT_PROTECTION_COMPAT_MODE)) {
-            si.immutabilityReason = IR_EXPLOIT_PROTECTION_COMPAT_MODE;
-            return false;
-        }
 
         return null;
@@ -43,7 +33,3 @@ protected boolean getDefaultValueInner(Context ctx, int userId, ApplicationInfo appInfo,
                                            GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            return true;
-        }
-
         si.defaultValueReason = DVR_DEFAULT_SETTING;
         return !ExtSettings.ALLOW_NATIVE_DEBUG_BY_DEFAULT.get(ctx);
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswRestrictMemoryDynCodeLoading.java b/frameworks/base/core/java/android/ext/settings/app/AswRestrictMemoryDynCodeLoading.java
index 2dd6e8d..0748ed7 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswRestrictMemoryDynCodeLoading.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswRestrictMemoryDynCodeLoading.java
@@ -7,7 +7,4 @@ import android.content.pm.GosPackageStateFlag;
 import android.content.pm.GosPackageStateFlag;
 import android.ext.settings.ExtSettings;
-import android.util.ArraySet;
 
-import com.android.internal.R;
-import com.android.internal.os.SELinuxFlags;
 import com.android.server.os.nano.AppCompatProtos;
@@ -26,14 +23,2 @@ private AswRestrictMemoryDynCodeLoading() {
 
-    private static volatile ArraySet<String> allowedSystemPkgs;
-
-    private static boolean shouldAllowByDefaultToSystemPkg(Context ctx, String pkg) {
-        var set = allowedSystemPkgs;
-        if (set == null) {
-            set = new ArraySet<>(ctx.getResources()
-                .getStringArray(R.array.system_pkgs_allowed_memory_dyn_code_loading_by_default));
-            allowedSystemPkgs = set;
-        }
-        return set.contains(pkg);
-    }
-
     @Override
@@ -41,19 +26,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            if (shouldAllowByDefaultToSystemPkg(ctx, appInfo.packageName)) {
-                // allow manual restriction
-                return null;
-            }
-            if (SELinuxFlags.isSystemAppSepolicyWeakeningAllowed()) {
-                return null;
-            }
-            si.immutabilityReason = IR_IS_SYSTEM_APP;
-            return true;
-        }
-
-        if (ps.hasFlag(GosPackageStateFlag.ENABLE_EXPLOIT_PROTECTION_COMPAT_MODE)) {
-            si.immutabilityReason = IR_EXPLOIT_PROTECTION_COMPAT_MODE;
-            return false;
-        }
 
         return null;
@@ -64,8 +33,4 @@ protected boolean getDefaultValueInner(Context ctx, int userId, ApplicationInfo appInfo,
                                            GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            return !shouldAllowByDefaultToSystemPkg(ctx, appInfo.packageName);
-        } else {
-            si.defaultValueReason = DVR_DEFAULT_SETTING;
-            return ExtSettings.RESTRICT_MEMORY_DYN_CODE_LOADING_BY_DEFAULT.get(ctx, userId);
-        }
+        si.defaultValueReason = DVR_DEFAULT_SETTING;
+        return ExtSettings.RESTRICT_MEMORY_DYN_CODE_LOADING_BY_DEFAULT.get(ctx, userId);
     }
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswRestrictStorageDynCodeLoading.java b/frameworks/base/core/java/android/ext/settings/app/AswRestrictStorageDynCodeLoading.java
index 31417bf..8cf0bc1 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswRestrictStorageDynCodeLoading.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswRestrictStorageDynCodeLoading.java
@@ -10,8 +10,5 @@ import android.ext.AppInfoExtFlag;
 import android.ext.AppInfoExtFlag;
 import android.ext.PackageId;
 import android.ext.settings.ExtSettings;
-import android.util.ArraySet;
 
-import com.android.internal.R;
-import com.android.internal.os.SELinuxFlags;
 import com.android.server.os.nano.AppCompatProtos;
@@ -29,14 +26,2 @@ private AswRestrictStorageDynCodeLoading() {
 
-    private static volatile ArraySet<String> allowedSystemPkgs;
-
-    private static boolean shouldAllowByDefaultToSystemPkg(Context ctx, String pkg) {
-        var set = allowedSystemPkgs;
-        if (set == null) {
-            set = new ArraySet<>(ctx.getResources()
-                .getStringArray(R.array.system_pkgs_allowed_storage_dyn_code_loading_by_default));
-            allowedSystemPkgs = set;
-        }
-        return set.contains(pkg);
-    }
-
     @Override
@@ -44,19 +29,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            if (shouldAllowByDefaultToSystemPkg(ctx, appInfo.packageName)) {
-                // allow manual restriction
-                return null;
-            }
-            if (SELinuxFlags.isSystemAppSepolicyWeakeningAllowed()) {
-                return null;
-            }
-            si.immutabilityReason = IR_IS_SYSTEM_APP;
-            return true;
-        }
-
-        if (ps.hasFlag(GosPackageStateFlag.ENABLE_EXPLOIT_PROTECTION_COMPAT_MODE)) {
-            si.immutabilityReason = IR_EXPLOIT_PROTECTION_COMPAT_MODE;
-            return false;
-        }
 
         return null;
@@ -77,11 +46,7 @@ protected boolean getDefaultValueInner(Context ctx, int userId, ApplicationInfo appInfo,
                                            GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            return !shouldAllowByDefaultToSystemPkg(ctx, appInfo.packageName);
-        } else {
-            if (appInfo.ext().hasFlag(AppInfoExtFlag.HAS_GMSCORE_CLIENT_LIBRARY)) {
-                if (isGmsCoreInstalled(ctx, userId)) {
-                    si.defaultValueReason = DVR_APP_IS_CLIENT_OF_GMSCORE;
-                    // Dynamite modules are loaded from writable data storage of GmsCore
-                    return false;
-                }
+        if (appInfo.ext().hasFlag(AppInfoExtFlag.HAS_GMSCORE_CLIENT_LIBRARY)) {
+            if (isGmsCoreInstalled(ctx, userId)) {
+                si.defaultValueReason = DVR_APP_IS_CLIENT_OF_GMSCORE;
+                // Dynamite modules are loaded from writable data storage of GmsCore
+                return false;
             }
@@ -88,5 +53,5 @@ protected boolean getDefaultValueInner(Context ctx, int userId, ApplicationInfo appInfo,
-
-            si.defaultValueReason = DVR_DEFAULT_SETTING;
-            return ExtSettings.RESTRICT_STORAGE_DYN_CODE_LOADING_BY_DEFAULT.get(ctx, userId);
         }
+
+        si.defaultValueReason = DVR_DEFAULT_SETTING;
+        return ExtSettings.RESTRICT_STORAGE_DYN_CODE_LOADING_BY_DEFAULT.get(ctx, userId);
     }
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswRestrictWebViewDynCodeLoading.java b/frameworks/base/core/java/android/ext/settings/app/AswRestrictWebViewDynCodeLoading.java
index 8ef413d..2f38060 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswRestrictWebViewDynCodeLoading.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswRestrictWebViewDynCodeLoading.java
@@ -7,7 +7,3 @@ import android.content.pm.GosPackageStateFlag;
 import android.content.pm.GosPackageStateFlag;
 import android.ext.settings.ExtSettings;
-import android.util.ArraySet;
-
-import com.android.internal.R;
-import com.android.internal.os.SELinuxFlags;
 
@@ -23,14 +19,2 @@ private AswRestrictWebViewDynCodeLoading() {
 
-    private static volatile ArraySet<String> allowedSystemPkgs;
-
-    private static boolean shouldAllowByDefaultToSystemPackage(Context ctx, String pkg) {
-        ArraySet<String> set = allowedSystemPkgs;
-        if (set == null) {
-            set = new ArraySet<>(ctx.getResources()
-                .getStringArray(R.array.system_pkgs_allowed_webview_dyn_code_loading_by_default));
-            allowedSystemPkgs = set;
-        }
-        return set.contains(pkg);
-    }
-
     @Override
@@ -38,14 +22,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            if (shouldAllowByDefaultToSystemPackage(ctx, appInfo.packageName)) {
-                // allow manual restriction
-                return null;
-            } else {
-                if (SELinuxFlags.isSystemAppSepolicyWeakeningAllowed()) {
-                    return null;
-                }
-                return true;
-            }
-        }
 
         return null;
@@ -56,7 +29,3 @@ protected boolean getDefaultValueInner(Context ctx, int userId, ApplicationInfo appInfo,
                                            GosPackageState ps, StateInfo si) {
-        if (appInfo.isSystemApp()) {
-            return !shouldAllowByDefaultToSystemPackage(ctx, appInfo.packageName);
-        } else {
-            return ExtSettings.RESTRICT_WEBVIEW_DYN_CODE_LOADING_BY_DEFAULT.get(ctx, userId);
-        }
+        return ExtSettings.RESTRICT_WEBVIEW_DYN_CODE_LOADING_BY_DEFAULT.get(ctx, userId);
     }
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswUseExtendedVaSpace.java b/frameworks/base/core/java/android/ext/settings/app/AswUseExtendedVaSpace.java
index 3f20df3..a22c8b3 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswUseExtendedVaSpace.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswUseExtendedVaSpace.java
@@ -26,18 +26,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        if (AswUseHardenedMalloc.I.get(ctx, userId, appInfo, ps)) {
-            si.immutabilityReason = IR_REQUIRED_BY_HARDENED_MALLOC;
-            return true;
-        }
-
-        String primaryAbi = appInfo.primaryCpuAbi;
-        if (primaryAbi != null && !VMRuntime.is64BitAbi(primaryAbi)) {
-            si.immutabilityReason = IR_NON_64_BIT_NATIVE_CODE;
-            return false;
-        }
-
-        if (ps.hasFlag(GosPackageStateFlag.ENABLE_EXPLOIT_PROTECTION_COMPAT_MODE)) {
-            si.immutabilityReason = IR_EXPLOIT_PROTECTION_COMPAT_MODE;
-            return false;
-        }
 
         return null;
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswUseHardenedMalloc.java b/frameworks/base/core/java/android/ext/settings/app/AswUseHardenedMalloc.java
index d02a8d2..75cb19b 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswUseHardenedMalloc.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswUseHardenedMalloc.java
@@ -10,5 +10,3 @@ import com.android.server.os.nano.AppCompatProtos;
 import com.android.server.os.nano.AppCompatProtos;
 
-import dalvik.system.VMRuntime;
-
 /** @hide */
@@ -26,31 +24,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        String primaryAbi = appInfo.primaryCpuAbi;
-        if (primaryAbi == null) {
-            si.immutabilityReason = IR_NO_NATIVE_CODE;
-            return true;
-        }
-
-        if (!VMRuntime.is64BitAbi(primaryAbi)) {
-            // hardened_malloc is 64-bit only
-            si.immutabilityReason = IR_NON_64_BIT_NATIVE_CODE;
-            return false;
-        }
-
-        if ((appInfo.flags & ApplicationInfo.FLAG_DEBUGGABLE) != 0) {
-            // turning off hardened_malloc requires exec spawning, which is always disabled for
-            // debuggable apps
-            si.immutabilityReason = IR_IS_DEBUGGABLE_APP;
-            return true;
-        }
-
-        if (appInfo.isSystemApp()) {
-            si.immutabilityReason = IR_IS_SYSTEM_APP;
-            return true;
-        }
-
-        if (ps.hasFlag(GosPackageStateFlag.ENABLE_EXPLOIT_PROTECTION_COMPAT_MODE)) {
-            si.immutabilityReason = IR_EXPLOIT_PROTECTION_COMPAT_MODE;
-            return false;
-        }
 
         return null;
@@ -61,3 +31,3 @@ protected boolean getDefaultValueInner(Context ctx, int userId, ApplicationInfo appInfo,
                                            GosPackageState ps, StateInfo si) {
-        return true;
+        return false;
     }
diff --git a/frameworks/base/core/java/android/ext/settings/app/AswUseMemoryTagging.java b/frameworks/base/core/java/android/ext/settings/app/AswUseMemoryTagging.java
index 1201529..79aa57c 100644
--- a/frameworks/base/core/java/android/ext/settings/app/AswUseMemoryTagging.java
+++ b/frameworks/base/core/java/android/ext/settings/app/AswUseMemoryTagging.java
@@ -29,33 +29,3 @@ public Boolean getImmutableValue(Context ctx, int userId, ApplicationInfo appInfo,
                                      GosPackageState ps, StateInfo si) {
-        final String primaryAbi = appInfo.primaryCpuAbi;
-        if (primaryAbi == null) {
-            si.immutabilityReason = IR_NO_NATIVE_CODE;
-            return true;
-        }
-
-        if (!VMRuntime.is64BitAbi(primaryAbi)) {
-            si.immutabilityReason = IR_NON_64_BIT_NATIVE_CODE;
-            return false;
-        }
-
-        if (appInfo.isSystemApp()) {
-            switch (appInfo.packageName) {
-                case PackageId.PIXEL_CAMERA_SERVICES_NAME:
-                    return false;
-            }
-            si.immutabilityReason = IR_IS_SYSTEM_APP;
-            return true;
-        }
-
-        int mm = appInfo.getMemtagMode();
-        if (mm == ApplicationInfo.MEMTAG_ASYNC || mm == ApplicationInfo.MEMTAG_SYNC) {
-            si.immutabilityReason = IR_OPTED_IN_VIA_MANIFEST;
-            return true;
-        }
-
-        if (ps.hasFlag(GosPackageStateFlag.ENABLE_EXPLOIT_PROTECTION_COMPAT_MODE)) {
-            si.immutabilityReason = IR_EXPLOIT_PROTECTION_COMPAT_MODE;
-            return false;
-        }
 
         return null;
diff --git a/frameworks/base/core/java/com/android/internal/os/ZygoteConnection.java b/frameworks/base/core/java/com/android/internal/os/ZygoteConnection.java
--- a/frameworks/base/core/java/com/android/internal/os/ZygoteConnection.java
+++ b/frameworks/base/core/java/com/android/internal/os/ZygoteConnection.java
@@ -56,6 +56,11 @@
 class ZygoteConnection {
     private static final String TAG = "Zygote";
 
+    // WizeOS Flexible: fully disable GrapheneOS exec-based app spawning for
+    // LSPosed / Xposed module compatibility. This keeps apps on the classic
+    // zygoteInit path and prevents the post-injection ExecInit re-exec.
+    private static final boolean WIZEOS_DISABLE_EXEC_SPAWNING_FOR_COMPAT = true;
+
     /**
      * The command socket.
      *
@@ -243,7 +248,8 @@ Runnable processCommand(ZygoteServer zygoteServer, boolean multipleOK) {
                     fdsToClose[1] = zygoteFd.getInt$();
                 }
 
-                if (parsedArgs.mInvokeWith != null || ExtSettings.EXEC_SPAWNING.get() || parsedArgs.mStartChildZygote
+                if (parsedArgs.mInvokeWith != null || WIZEOS_DISABLE_EXEC_SPAWNING_FOR_COMPAT
+                        || ExtSettings.EXEC_SPAWNING.get() || parsedArgs.mStartChildZygote
                         || !multipleOK || peer.getUid() != Process.SYSTEM_UID
                         || (parsedArgs.mRuntimeFlags & Zygote.RUNTIME_FLAGS_DEPENDENT_ON_EXEC_SPAWNING) != 0) {
                     Log.w(TAG, "Resorting to Java fork code; multipleOK = " + multipleOK
@@ -531,11 +537,10 @@ private Runnable handleChildProc(ZygoteArguments parsedArgs,
         } else {
             if (!isZygote) {
                 final int runtimeFlags = parsedArgs.mRuntimeFlags;
-                boolean useExecInit =
-                        ((runtimeFlags & Zygote.RUNTIME_FLAGS_DEPENDENT_ON_EXEC_SPAWNING) != 0
+                boolean useExecInit = !WIZEOS_DISABLE_EXEC_SPAWNING_FOR_COMPAT
+                        && ((runtimeFlags & Zygote.RUNTIME_FLAGS_DEPENDENT_ON_EXEC_SPAWNING) != 0
                             || ExtSettings.EXEC_SPAWNING.get())
-                        &&
-                        (runtimeFlags & ApplicationInfo.FLAG_DEBUGGABLE) == 0;
+                        && (runtimeFlags & ApplicationInfo.FLAG_DEBUGGABLE) == 0;
 
                 if (useExecInit) {
                     ExecInit.execApplication(parsedArgs.mNiceName, parsedArgs.mTargetSdkVersion,
@@ -560,8 +565,8 @@ private Runnable handleChildProc(ZygoteArguments parsedArgs,
     /**
      * Handles post-fork cleanup of parent proc
      *
-     * @param pid != 0; pid of child if &gt; 0 or indication of failed fork
-     * if &lt; 0;
+     * @param pid != 0; pid of child if > 0 or indication of failed fork
+     * if < 0;
      * @param pipeFd null-ok; pipe for communication with child.
      */
     private void handleParentProc(int pid, FileDescriptor pipeFd) {
@@ -580,7 +585,6 @@ private void handleParentProc(int pid, FileDescriptor pipeFd) {
                 StructPollfd[] fds = new StructPollfd[] {
                         new StructPollfd()
                 };
-
                 byte[] data = new byte[BYTES_REQUIRED];
 
                 int remainingSleepTime = WRAPPED_PID_TIMEOUT_MILLIS;
