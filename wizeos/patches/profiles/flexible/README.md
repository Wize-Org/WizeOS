# Flexible profile patches

Place flexible-only `.patch` or `.diff` files here.

This profile is intended for compatibility-first changes on top of the GrapheneOS base. SELinux should remain enforcing; compatibility changes should follow AOSP/LineageOS-style behavior instead of global permissive policy.
